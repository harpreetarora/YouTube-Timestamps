# This file documents environment variables for local development.
# Copy to .env and adjust as needed. Do not commit real secrets.

# When set to "true", the app will render a test-only mock vulnerability
# component that intentionally uses dangerouslySetInnerHTML to simulate XSS.
# WARNING: Never enable this in production.
REACT_APP_ENABLE_MOCK_VULN=false

